# Engineering Harness

This directory is the persistent state of work governed by `arm-full-cycle`.

Start with `status.md`, update the relevant phase artifact as decisions and evidence emerge, and add an ADR for consequential choices. Mark irrelevant phases as `not_applicable`; do not delete their records.
